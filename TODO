cookie control
userjs
blocking UI
inspector
session saving
pressing enter when searching should go to the link, not next match
previous button opens a menu of last few?
FLTK tooltip @symbols
date crash
click vs highlight pos difference in tabs
warn about unicode urls
JS ecmascript 6 let, arrow funcs, performance/user timing api, fetch
!function(){try{new Function("(a=0)=>a"),!0}catch(n){alert("fail")}}();
if (typeof performance == "undefined") alert("no performance")
shorten Webcore: thread prefix
per-script cpu use, disabling if too much
dragging text in the search field needs a full refresh
share download cookies
kickstarter error console crashes
