cookie control
more cert info in SSL error
userjs
blocking UI
inspector
session saving
pressing enter when searching should go to the link, not next match
previous button opens a menu of last few?
FLTK tooltip @symbols
date crash
new download tab should be auto-closed (test with orava)
enable webkit req anim frame
click vs highlight pos difference in tabs
shorten long titles in closed tabs
if a download tab goes ssl block, don't clean it up
inline script blocking
hard-whitelist *.gstatic.com, useless to check for; same if both are let's encrypt
